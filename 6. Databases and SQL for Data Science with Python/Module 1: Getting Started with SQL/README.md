# 🗄 Module 1: Databases and SQL for Data Science with Python 

**Course:** Databases and SQL for Data Science with Python (Course 6)  
**Platform:** Coursera  

---

## 🔹 Module Overview
Module 1 introduces learners to the fundamentals of databases and SQL for data science. Learners explore basic SQL statements such as SELECT, INSERT, UPDATE, and DELETE, gaining practical experience in querying and modifying data.

The module also covers techniques to refine query results using the WHERE clause, and aggregate functions such as COUNT, DISTINCT, and LIMIT. Hands-on exercises allow learners to interact with tables and apply SQL statements to real datasets.

> This module provides foundational knowledge of SQL to retrieve, modify, and analyze data effectively for data science applications.

---

## 🎯 Learning Objectives
By completing this module, learners will be able to:

1. Define databases and SQL.  
2. Write basic data retrieval queries using the `SELECT` statement.  
3. Refine query results using `COUNT`, `DISTINCT`, and `LIMIT`.  
4. Insert data into tables using `INSERT` statements and modify or remove data with `UPDATE` and `DELETE` statements.  

---

## 📝 Key Topics
- Introduction to databases and SQL  
- Retrieving data with `SELECT` statements  
- Refining query results using `WHERE`, `COUNT`, `DISTINCT`, and `LIMIT`  
- Inserting, updating, and deleting data in tables  
- Hands-on exercises with real datasets  

---

